import React,{useContext} from 'react'
import { StyleSheet, Text } from 'react-native'

const MyText2=()=>{
    // const contextType = Context;
 
    return(
        <Text  style={{fontSize:14}} >alp</Text>
    )
}
export default MyText2
